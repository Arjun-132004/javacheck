apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: javacheck-backend-postgres-password
  namespace: storefront-developer
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: deepagent-aws
    kind: ClusterSecretStore
  target:
    name: app-env
    creationPolicy: Merge
  data:
    - secretKey: POSTGRES_PASSWORD
      remoteRef:
        key: t5-dev/storefront-developer/POSTGRES_PASSWORD
